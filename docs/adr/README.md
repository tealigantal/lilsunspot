# Architecture Decision Records

当工作改变模块或数据所有权、公共接口、安全边界、持久化、主要依赖、部署拓扑或长期兼容策略时，新增 ADR。每份 ADR必须包含 Status、Context、Considered Options、Decision、Rationale、Consequences、Migration、Rollback、Date。

- `0001-generation-control-resolution-and-provider-compatibility.md`
