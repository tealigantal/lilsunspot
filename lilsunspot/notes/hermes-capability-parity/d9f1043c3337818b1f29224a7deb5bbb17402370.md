# Hermes capability parity ledger

Date: 2026-07-26
Target: `d9f1043c3337818b1f29224a7deb5bbb17402370`
Task: `LIL-HERMES-UPSTREAM-FULL-SYNC-01`

## Checkpoint

The fixed-target machine inventory now expands to 519 stable capability rows:

| Kind | Rows |
| --- | ---: |
| Toolsets | 57 |
| Built-in tools | 74 |
| Plugins | 93 |
| Skills | 180 |
| Optional MCPs | 4 |
| Gateway platforms | 30 |
| Provider transports | 4 |
| User-facing config surfaces | 76 |
| Internal config surfaces | 1 |

Current mapping state is `135 design_mapped / 384 unspecified`; current evidence state is `0 validated / 519 needs_validation`. All 57 toolsets, 74 static tools and four concrete provider transports have explicit responsibility mappings but remain `blocked_not_merged`. Both `mapping_complete` and `ready_complete` are false. Inventory presence is not treated as product integration or installed availability.

## Stable identity rules

- Toolsets and tools use their official registry names.
- Plugins use category plus canonical `plugin.yaml` name; source paths are evidence, not identity.
- Skills use scope plus canonical frontmatter name; duplicate canonical IDs fail generation.
- Gateways use the official platform key. The ledger includes 9 built-in platforms and 21 plugin platforms, including separate `wecom` and `wecom_callback` rows.
- Provider transports include only the four concrete direct `ProviderTransport` implementations and use stable protocol keys: `anthropic`, `bedrock`, `chat_completions`, `responses_api`. Helper modules are excluded.
- `_config_version` is classified as an internal config surface instead of a user capability.

## Mapping and readiness gates

Each row remains `unspecified` until it has all of:

- lilsunspot owner;
- one or more product-entry references;
- one or more configuration-source references;
- one or more safety-policy references;
- a typed packaging state;
- one or more validation-method references.

`design_mapped` means only that these responsibility fields are specified; it does not mean integrated or validated. `parity_status` is restricted to `verified_available`, `verified_after_configuration`, `platform_limited_with_solution`, or `upstream_unavailable`, and can only be accepted with corresponding evidence. Installed availability additionally requires bundle presence, installed discovery, installed invocation, explicit configuration/safety/negative-test assessment, a verification timestamp and evidence IDs. `--require-parity-complete` requires both mapping and validation completion.

## P0 conservative boundaries

The following categories must not be bulk-marked mapped or packaged-ready from source presence: all plugins, all skills, all MCPs, all provider transports, all plugin gateways, every non-Weixin built-in gateway, high-risk or dependency-backed tools/toolsets, operator CLI/Web/TUI surfaces, and config keys without a proven consumer and product entry. Weixin itself must be split and evidenced across login, inbound text, Agent reply, proactive send and media delivery rather than validated from `connected=true`.

The current sidecar does not collect complete `plugins/`, skills, optional MCP resources or the full gateway package. The product provider registry also exposes only six UX records. These remain Milestone 3-5 implementation blockers, not ledger completion shortcuts.

## Validation

- Audit schema tests: 6 passed.
- Capability/product/audit focused regression: 29 passed.
- Product upstream-check script: 3 passed using the Windows thread timeout method.
- Strict completion gate remains expected non-zero; after the toolset/tool/transport slice it reports `384 mapping-unspecified rows`.
- `scripts/check.ps1`: daemon 160 passed and secret guard passed; desktop build skipped because this isolated worktree has no untracked `node_modules`.
- Independent read-only review: no remaining P0 mapping blocker in the toolset/tool/transport slice.

## Next slice

The 77 config rows remain `unspecified`. A read-only target/current audit found target `_config_version=33` but no product-layer version gate, schema validation, backup/rollback, downgrade rejection or transactional config-plus-secret migration. The target model/provider/secret shapes also differ from the current product dict and `.env` adapter, while legacy `platform_toolsets`, `mcp_servers` and `provider_routing` lack migration contracts. Preserve this audit checkpoint, merge the fixed SHA, implement the v33 migration contract, and only then accept config mappings. Continue plugins, skills, MCP and gateway mappings afterward. Do not enable the strict completion gate in the normal check until all 519 rows have deliberate mappings; use the gate as the Milestone 2 exit check.
